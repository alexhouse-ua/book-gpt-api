{
  "version": 2,
  "builds": [
    { "src": "api/**/*.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/internal/(.*)", "dest": "api/maintenance/[task].py" },
    { "src": "/api/(.*)",      "dest": "api/$1.py" }
  ]
}